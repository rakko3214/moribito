{
  "name": "@moribito/save-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": { "dev": "tsx watch src/server.ts", "build": "tsc -p tsconfig.json", "typecheck": "tsc -p tsconfig.json --noEmit", "test": "vitest --exclude dist/**", "test:run": "vitest run --exclude dist/**" },
  "dependencies": { "@moribito/shared": "0.1.0" },
  "devDependencies": { "tsx": "4.20.6" }
}
